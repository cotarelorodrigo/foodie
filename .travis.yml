language: python

python:
  - "3.6"

services:
  - docker

install:
  - pip install -r ./python-apirest/requirements.txt

before_script:
  - sh build.sh
  - sh run.sh

script:
  - python -m pytest -v
  - coveralls      

after_script:
  - sh stop.sh
